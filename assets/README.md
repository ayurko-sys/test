<!-- PLACEHOLDER — safe to delete once you add the real asset. -->

# assets/  (currently empty)

Put shared binary assets here. At minimum:

- `logo.png` — used for the header, the `Organization` structured-data `logo`,
  and the OG image on every page.

(Delete this placeholder note when you add the real assets.)

<!-- PLACEHOLDER — safe to delete once you drop a real export in here. -->

# sitegen-sample/  (REQUIRED INPUT — currently empty)

**This is the single most important input in the pipeline, and it is missing.**

Drop an export of **any** finished page from the sitegen editor here — even the
raw 5-page template sitegen generated for you is a perfect sample. It's the only
way to learn sitegen's internal format; it must not be guessed.

Accepts whatever sitegen exports: a `.zip` (unpack it here), loose `.html`, or a
folder of block files. Multiple pages are welcome.

Once this folder has real content, the first workflow step can run:

> Read CLAUDE.md. Start with step 1 of the Workflow: reverse-engineer the sitegen
> format from /sitegen-sample/ and write SITEGEN-NOTES.md. Don't convert anything yet.

Per CLAUDE.md this folder is **read-only** — Claude reverse-engineers from it but
never edits it. (Delete this placeholder note when you add the real export.)

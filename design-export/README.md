<!-- PLACEHOLDER — safe to delete once you drop a real export in here. -->

# design-export/  (REQUIRED INPUT — currently empty)

Unpack the **Claude Design** export here (the ZIP/HTML of the site visuals).
This is the source of truth for layout and styling that gets adapted into
sitegen blocks in Step 2 of the Workflow.

Per CLAUDE.md this folder is **read-only** — Claude reads visuals from it but
never edits it; all adapted output goes to `/dist/`. (Delete this placeholder
note when you add the real export.)
